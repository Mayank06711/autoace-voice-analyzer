# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# secrets & local state (never commit)
.env
*.db
*.db-wal
*.db-shm
storage/
logs/
*.env
# large model weights (baked at build / downloaded; not tracked)
models/*.onnx
.claude/
# confidential assignment doc + scratch (do not redistribute)
brief.pdf
.firecrawl/
data/

# os / tooling noise
.DS_Store
*.output
sample_batch.zip

# internal build artifacts (not brief deliverables)
CLAUDE.md
docs/PLAN.md
docs/BUILD_LOG.md

# local test samples (not for the public repo)
samples_test/
